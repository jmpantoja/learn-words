version: '3.4'
services:
  php:
    environment:
      - APP_ENV=prod
      - APP_DEBUG=false

  proxy:
    environment:
      CLIENT_MAX_BODY_SIZE: 64M
      DOMAINS: "{{ macros.domains(APP_HOST, sites, 'production') }}"
      
  nuxt:
    command:
      "yarn build && yarn start"
