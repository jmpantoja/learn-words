Entry:
    type: object
    config:
        description: "An entry"
        fields:
            id:
                type: "String!"
                description: "The ID of the entry."
            word:
                type: "Word"
                description: "the word"
                                
